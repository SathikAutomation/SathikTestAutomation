name: Playwright Prod Tests

on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    - cron: "0 3 * * *"

jobs:
  run-prod-tests:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        project: [US_Prod_Desktop, US_Prod_Mobile, CA_Prod_Desktop, CA_Prod_Mobile]

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4  

      - name: Setup Node.js
        uses: actions/setup-node@v4  
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright Tests for Prod (${{ matrix.project }})
        run: npx playwright test --project=${{ matrix.project }}

      - name: Upload Playwright Test Results
        if: always()
        uses: actions/upload-artifact@v4  
        with:
          name: playwright-prod-results-${{ matrix.project }}
          path: playwright-report/

      - name: Send Email Notification
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.yourcompany.com
          server_port: 25  # No authentication required
          subject: "Playwright Test Report - Prod (${{ matrix.project }})"
          body: "Playwright test execution has completed. Please find the attached report."
          to: |
            sathiksha.sf@gmail.com
            sathiksha.sb@gmail.com
            sathik.a@vuedata.in
          from: "noreply@company.com"
          attachments: playwright-report/index.html
